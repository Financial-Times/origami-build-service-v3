version: 2


jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run: npm install
      - run: make verify
      - run: make test

workflows:
  version: 2
  test:
    jobs:
      - build
